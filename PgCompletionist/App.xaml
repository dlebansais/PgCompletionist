<Application x:Class="PgCompletionist.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Converters;assembly=XamlConverterLibrary"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:PgCompletionist"
             Startup="OnStartup">
    <Application.Resources>
        <conv:BooleanToObjectConverter x:Key="convBooleanToObject"/>
        <CompositeCollection x:Key="CollapsedVisible">
            <Visibility>Collapsed</Visibility>
            <Visibility>Visible</Visibility>
        </CompositeCollection>
        <CompositeCollection x:Key="TrueFalse">
            <sys:Boolean>True</sys:Boolean>
            <sys:Boolean>False</sys:Boolean>
        </CompositeCollection>
        <RoutedCommand x:Key="AddReportCommand"/>
        <DataTemplate x:Key="CharacterNameTemplate" DataType="{x:Type local:Character}">
            <TextBlock Text="{Binding Name}" FontSize="16"/>
        </DataTemplate>
        <DataTemplate x:Key="MissingAbilitiesTemplate" DataType="{x:Type local:Character}">
            <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="16"/>
        </DataTemplate>
        <DataTemplate x:Key="CharacterTemplate" DataType="{x:Type local:Character}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" Text="Name:" FontSize="16" TextAlignment="Right" Margin="0,0,0,0"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Name}" FontSize="16" Margin="5,0,0,0"/>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Missing Skills:" FontSize="16" TextAlignment="Right" Margin="0,5,0,0"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding MissingSkills}" FontSize="16" TextWrapping="Wrap" Margin="5,5,0,0"/>
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Non-maxed Skills:" TextAlignment="Right" FontSize="16" Margin="0,5,0,0"/>
                <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding NonMaxedSkills}" TextWrapping="Wrap" FontSize="16" Margin="5,5,0,0"/>
                <TextBlock Grid.Column="0" Grid.Row="3" Text="Missing Abilities:" TextAlignment="Right" FontSize="16" Margin="0,5,0,0"/>
                <ItemsControl Grid.Column="1" Grid.Row="3" ItemsSource="{Binding MissingAbilitiesList}" ItemTemplate="{StaticResource MissingAbilitiesTemplate}" Margin="5,5,0,0"/>
                <TextBlock Grid.Column="0" Grid.Row="4" Text="Missing Recipes:" TextAlignment="Right" FontSize="16" Margin="0,5,0,0"/>
                <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding MissingRecipes}" TextWrapping="Wrap" FontSize="16" Margin="5,5,0,0"/>
            </Grid>
        </DataTemplate>
    </Application.Resources>
</Application>
